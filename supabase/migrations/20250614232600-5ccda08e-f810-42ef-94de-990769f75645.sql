
-- Remove the reward_distributions table first (due to foreign key dependency)
DROP TABLE IF EXISTS public.reward_distributions;

-- Remove the reward_campaigns table
DROP TABLE IF EXISTS public.reward_campaigns;
